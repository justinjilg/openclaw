###############################################################################
# OpenClaw — Hardened Docker Compose
#
# Security layers applied:
#   - Loopback-only port binding (no external access)
#   - Non-root user (node/UID 1000)
#   - All Linux capabilities dropped
#   - no-new-privileges
#   - Custom seccomp profile (syscall allowlist)
#   - Resource limits (memory, CPU, PIDs)
#   - Ulimits (prevent fork bombs)
#   - Log rotation
#
# Usage:
#   1. ./start.sh up        (injects secrets from 1Password, starts gateway)
#   2. ./start.sh doctor    (security audit)
###############################################################################

services:
  openclaw-gateway:
    image: ghcr.io/openclaw/openclaw:2026.4.29
    container_name: openclaw-gateway
    restart: unless-stopped
    command: ["openclaw", "gateway", "--verbose"]

    ports:
      # Bind to loopback ONLY — never expose on 0.0.0.0
      - "127.0.0.1:18789:18789"   # Gateway (WebSocket + HTTP + Control UI)
      - "127.0.0.1:18790:18790"   # Bridge

    volumes:
      # Mount config + workspace to /home/node (container's node user home)
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE:-./workspace}:/home/node/workspace
      # Plugin runtime-deps cache lives on a Linux-native volume, not the macOS
      # bind mount — pnpm hits EPERM on copyfile across Docker Desktop's file
      # sharing for the per-version, per-plugin staging tree introduced in
      # 2026.4.29. The cache is regeneratable; sessions/memories stay in the
      # bind mount above.
      - openclaw-plugin-runtime-deps:/home/node/.openclaw/plugin-runtime-deps
      # Cross-project read-only access (Phase 3: agent workforce)
      - ${PROJECTS_DIR:-~/Projects}:/home/node/projects:ro

    environment:
      - HOME=/home/node
      # Auth token — injected at runtime from 1Password via start.sh
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
      # LLM provider credentials
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY}
      - BRAINSTORMROUTER_API_KEY=${BRAINSTORMROUTER_API_KEY}
      - BRAINSTORMROUTER_ADMIN_KEY=${BRAINSTORMROUTER_ADMIN_KEY}
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      # GitHub access for filing issues, reading repos
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      # WhatsApp bridge needs more heap than Node.js default
      - NODE_OPTIONS=--max-old-space-size=1792
      # Disable insecure private WebSocket
      - OPENCLAW_ALLOW_INSECURE_PRIVATE_WS=false
      # Startup optimization (from openclaw doctor)
      - OPENCLAW_NO_RESPAWN=1

    # Run as non-root (node user = UID 1000)
    user: "1000:1000"

    # Read-only root filesystem (writable paths via tmpfs)
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
      - /home/node/.cache:size=64m,mode=0700

    # Drop all capabilities
    cap_drop:
      - ALL

    # Prevent privilege escalation + custom seccomp syscall filter
    security_opt:
      - no-new-privileges:true
      - seccomp:openclaw-seccomp.json

    # Prevent fork bombs
    ulimits:
      nproc: 256
      nofile:
        soft: 8192
        hard: 16384

    # Resource limits
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
          pids: 256
        reservations:
          memory: 512m

    healthcheck:
      test: ["CMD", "curl", "-4sf", "http://127.0.0.1:18789/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 240s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # --- Custom BR-Integrated Dashboard ---
  br-dashboard:
    # Pinned by digest (nginxinc/nginx-unprivileged:alpine as of 2026-04-10)
    image: nginxinc/nginx-unprivileged@sha256:601c823234c474696ded03d619e67f1e59538802731543099c691aea67e4a553
    container_name: br-dashboard
    restart: unless-stopped
    profiles: ["dashboard"]
    ports:
      - "127.0.0.1:3001:8080"
    volumes:
      - ./workspace/dashboard:/usr/share/nginx/html:ro
    depends_on:
      openclaw-gateway:
        condition: service_healthy
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
      - seccomp:openclaw-seccomp.json
    deploy:
      resources:
        limits:
          memory: 128m
          cpus: "0.25"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"

  openclaw-cli:
    image: ghcr.io/openclaw/openclaw:2026.4.29
    container_name: openclaw-cli
    profiles: ["cli"]
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE:-./workspace}:/home/node/workspace
      - openclaw-plugin-runtime-deps:/home/node/.openclaw/plugin-runtime-deps
      - ${PROJECTS_DIR:-~/Projects}:/home/node/projects:ro
    environment:
      - HOME=/home/node
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY}
      - BRAINSTORMROUTER_API_KEY=${BRAINSTORMROUTER_API_KEY}
      - BRAINSTORMROUTER_ADMIN_KEY=${BRAINSTORMROUTER_ADMIN_KEY}
    user: "1000:1000"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
      - seccomp:openclaw-seccomp.json

volumes:
  openclaw-plugin-runtime-deps:
    # Per-version pnpm cache. Linux-native to avoid macOS Docker Desktop
    # bind-mount EPERM. Safe to delete with `docker compose down -v` — gateway
    # will rebuild on next start.
