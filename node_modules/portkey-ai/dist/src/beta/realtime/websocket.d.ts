import { OpenAIRealtimeWebSocket } from 'openai/beta/realtime/websocket';
export declare class PorkteyRealtimeWebSocket extends OpenAIRealtimeWebSocket {
    url: URL;
    socket: any;
    constructor(props: {
        model: string;
        options?: any;
    }, client: any);
}
